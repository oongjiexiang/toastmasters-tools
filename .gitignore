node_modules/
/.vscode/
# package-lock.json is committed: CI (npm ci + setup-node cache) requires it.
*.data.json
.env
*.csv
*.sqlite
tmp*

# Next.js build output
.next/
out/

# TypeScript incremental build cache
*.tsbuildinfo

# Vitest coverage output
coverage/

# General build output
dist/

# electron-builder output (apps/desktop) — the packaged .exe
release/

# Playwright output
test-results/
playwright-report/